# 红楼实际开局与首轮耗时（2026-09-25）

在本机 Chrome 的真实酒馆页面 http://127.0.0.1:3081 操作，重启 CLI 服务加载最新代码后，新开红楼卡。前台为新局默认 DeepSeek-V4-Flash / High，没有沿用旧局的 Pro。使用既有资源缓存和真实配置 API，无模拟模型。Session：`session-e7abb94f-47fc-4e5f-b252-c8aac38ad398`。

用户输入：“我起身向母亲和姨妈问安，询问今日府里的安排，随后到院子里看看天气。”

## 开局

| 环节 | 实测 |
| --- | ---: |
| 点击红楼卡至预览数据就绪 | 72 ms |
| getCardOpenings 服务端 | 20 ms |
| 其中读卡 / 扩展 / 远程资源 / 预览 | 6 / 2 / 0 / 3 ms |
| 点击开始游戏至打开会话 | 1133 ms |
| 等待预热 | 7 ms |
| 连接工作区、创建 Session | 166 ms |
| 等待 Session / 设置预设 | 1 / 25 ms |
| startChat 浏览器 RPC | 333 ms |
| startChat 服务端 | 177 ms |
| 同步并打开会话 | 596 ms |

父子阶段有重叠，不能把整表相加。读卡准备和点击开始之间的人工等待不计入初始化。

## 首轮

Session turn 1 是预置开场，首次真正调用模型的是 turn 2。

| 事件 | UTC 时间 | 距 turn/start |
| --- | --- | ---: |
| turn/start | 14:12:11.974 | 0 |
| step/start | 14:12:12.273 | 0.299 s |
| request/header | 14:12:12.276 | 0.302 s |
| 首个思考增量 | 14:12:14.929 | 2.955 s |
| 首个正文增量 | 14:12:37.578 | 25.604 s |
| 模型流完成 | 14:12:41.636 | 29.662 s |
| turn/end | 14:12:41.719 | 29.745 s |

浏览器自动化在发送点击调用前记录的时间为 14:12:11.472，因此“点击到事件”约多 0.502 秒，包含自动化派发开销，不能视为精确 DOM 点击处理耗时。流时间是宿主接收到增量的时间，不是浏览器绘制时间。

后台结算 turn/start 14:12:41.955，turn/end 14:12:45.373，持续 3.418 秒。前台 turn/start 至后台完成共 33.399 秒。后台仅一轮模型步骤，页面最终显示“后台结算已完成”。

首轮 inputTokens 5702、cacheReadTokens 2176、outputTokens 4620，其中 reasoningTokens 3815。实际主要耗时在模型思考（首个思考到正文约 22.65 秒），正文流输出约 4.06 秒；请求前本地准备约 0.30 秒。此次没有复现本地初始化几十秒卡顿，单次本机结果不能代表用户 Windows/手机或其他 API。

## 其他观察

- 先打开的创世回廊预览准备为 1901 ms（资源已有缓存）；模板初始化 RPC 1381 ms，其中服务端模板阶段 1054 ms。按用户要求放弃该卡，未把它算作完整开局测试。
- 实际刷新发现状态面板空 view 分支误读 activity，已在 b548c408 修复并在原页面确认恢复。
- 本次导出的浏览器 startedAt/sentAt 被旧的 1e12 数值上限截断；阶段 durationMs 和 Session/server 时间正确。已补修复及回归测试，原诊断包保持原样，未伪造其中绝对时间。

诊断来源：用户 Downloads 下 dsh-tavern-diagnostics-session-e7abb94f-47fc-4e5f-b252-c8aac38ad398.zip，包含 performance/summary.json、前台和后台 session.jsonl。本文仅摘录性能指标，不复制会话正文、思考过程或 API 凭据。
